// Step 12 — async/await + tokio. This file is YOURS: the worksheet
// (../README.md) rebuilds it move by move. It compiles as-is — `cargo run`
// from the step folder is move 1.
//
// NOTE: `fn main` below is deliberately NOT async. Moves 2 and 3 walk you
// into two compiler errors from exactly here; move 4 earns the fix.

fn main() {
    println!("step 12: hello from cargo — tokio is compiled and waiting");
}
